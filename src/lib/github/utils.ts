export const repo = {
  owner: "guidopenta",
  name: "galactic-cv-guide",
  branch: "main",
  file: "index.md"
}

export const headers = {
  Authorization: `Bearer ${import.meta.env.GITHUB_ACCESS_TOKEN}`
}
